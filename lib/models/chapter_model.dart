class ChapterModel {
  num? Id;
  num? bookId;
  String? bookName;
  String? title;
  num? chapterId;
  num? number;
  String? hadisRange;
  ChapterModel(this.Id, this.bookId, this.bookName, this.title, this.chapterId,
      this.number, this.hadisRange);

}
